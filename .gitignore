node_modules
.next
.env
.env.local
.env.*.local
# Allow committed example files
!env.example

scripts/.smoke-secret

# TypeScript incremental build cache (regenerated by `tsc`)
tsconfig.tsbuildinfo

# Local redirect/capture logs from one-off `npm run` invocations
scripts/*-out.txt
.vercel
.env*
_debug_token.txt
