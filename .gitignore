node_modules
.next
.env
.env.local
.env.*.local
# Allow committed example files
!env.example

scripts/.smoke-secret

# TypeScript incremental build cache (regenerated by `tsc`)
tsconfig.tsbuildinfo

# Local redirect/capture logs from one-off `npm run` invocations
scripts/*-out.txt
.vercel
.env*
_debug_token.txt

# One-off capture files from manual curl/debug runs. `_tok.txt` has held a
# live session token before now, so these must never be committed.
_tok.txt
body.txt
headers.txt
